<%= simple_form_for [current_user, @habit] do |f| %>

  <% if @habit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@habit.errors.count, "error") %> prohibited this budget item from being saved:</h2>

      <ul>
      <% @habit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="form-field">
    <%= f.input :frequency, collection: ["Daily", "Weekly", "Monthly"], as: :radio_buttons, required: true, item_wrapper_class: "inline" %>

    <%= f.input :title %>
  </p>

  <p class="form-field">
    <%= f.input :description %>
  </p>

  <p class="form-field">
    <%= f.button :submit, class: "form-buttons" %>
  </p>
<% end %>
